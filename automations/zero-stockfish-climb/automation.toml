version = 1
id = "zero-stockfish-climb"
kind = "cron"
name = "zero-stockfish-climb"
prompt = '''
Run as a fast reporter/watchdog for the repo-local Codex-chess-zero climb loop, then exit. Do not run a climb sprint directly, do not edit research code, and do not wait for a full training round to finish.

First read `C:\Users\marvi\.codex\automations\zero-stockfish-climb\memory.md` so the report builds on the previous automation state.

From `C:\dev\chess-harness-codex`, run:
python .\tools\ensure_zero_climb_loop.py --profile gm-sprint --wait-seconds 20

The helper should detect an existing `tools/run_zero_climb_loop.py` process, or start it detached if absent. It may repair a stale loop lock only after confirming no loop process is alive. It writes `engines/codex-chess-zero/research/climb/zero-climb-loop-ensure-log.jsonl` and the loop writes its own state/log/lock artifacts.

After the helper returns, inspect the current local artifacts if present:
- `engines/codex-chess-zero/research/climb/zero-climb-loop-state.json`
- `engines/codex-chess-zero/research/climb/zero-climb-loop-log.jsonl`
- `engines/codex-chess-zero/research/climb/climb-state.json`
- `engines/codex-chess-zero/research/climb/climb-log.jsonl`
- `engines/codex-chess-zero/research/wisdom/latest-wisdom-delta.md`
- `engines/codex-chess-zero/research/networks`

Append a concise memory entry with the current run time, loop status/PID, current stage, latest score, promotion result, artifact paths, and whether any external-label guard failed. If a field is not available yet because the loop is still mid-round, say so honestly. Report the same concise status to the user. If the helper fails, report the failure, append that failure to memory, and do not attempt manual training in this automation.
'''
status = "ACTIVE"
rrule = "FREQ=MINUTELY;INTERVAL=15"
target_thread_id = "019ea954-4107-7632-9ec6-b21f6f12f6eb"
model = "gpt-5.3-codex"
reasoning_effort = "low"
execution_environment = "local"
cwds = ["C:/dev/chess-harness-codex"]
